# Core dependencies installed with `pip install flexipipe`
pybind11>=2.10
langcodes>=3.3.0
language-data>=1.1.0
pycountry>=23.12.0
fasttext-numpy2>=0.9.2.post2
requests>=2.31.0
tabulate>=0.9.0

# Optional extras (install on demand)
# pip install "flexipipe[spacy]"
# pip install "flexipipe[stanza]"
# pip install "flexipipe[transformers]"
# pip install "flexipipe[all]"

# Development helpers
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0
